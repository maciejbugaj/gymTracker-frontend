# Not needed for local dev (Vite proxy handles /api -> localhost:8080).
# In Railway, set this as a Build Variable on the frontend service:
#   e.g. https://your-backend.up.railway.app/api
VITE_API_BASE_URL=

VITE_KEYCLOAK_URL=
VITE_KEYCLOAK_REALM=
VITE_KEYCLOAK_CLIENT_ID=